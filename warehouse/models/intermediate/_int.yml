version: 2

models:
  - name: int_service
    description: |
      All service from active feeds.
    tests:
      - dbt_utils.unique_combination_of_columns:
            combination_of_columns:
              - service_id
              - service_date
              - _b64_url
